# Release Packaging

This folder is used for generated release packages.

For Windows release packaging, run:

```powershell
.\scripts\package_windows_release.ps1
```

This creates:

- `release/windows/SuDel-windows/`
- `release/windows/SuDel-windows.zip`
